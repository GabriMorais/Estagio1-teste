var config = {
    type: Phaser.AUTO,
    width: 1350,
    height: 520,
    
    backgroundColor: 0x000000,
    physics: {
        default: 'arcade'
    },
    pixelArt: true
};

var game = new Phaser.Game(config);
